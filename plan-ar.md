# مراجعة كود احترافية — الجولة الثانية: تدفق إنشاء الحجز (`feat/reservation`)

- **الفرع:** `feat/reservation`
- **نطاق المراجعة (الجولة الثانية — التغييرات المعلّقة المحدَّثة):**
  - `reservation.entity.ts` (فهرس فريد جزئي + قيمة افتراضية للحالة)، الهجرة الجديدة `1786000000001-add-reservations-shift-date-active-unique-index.ts`
  - `reservations.service.ts` (أصبح يمتد `BaseEntityService` ويلتقط انتهاك التفرد في PostgreSQL)
  - `reservations.controller.ts`, `reservations.module.ts`, `shift.module.ts`, `create-reservation.dto.ts`
  - `is-not-past-date.decorator.ts` (أُعيدت كتابته)، `is-bigint-id.decorator.ts` (رسالة مترجمة)، و`src/libs/utils/postgres-error.ts` الجديد
  - `src/i18n/{en,ar}/common.json` (إضافة `validation.*`)، و`src/i18n/{en,ar}/reservations.json`، وملفّا اختبارات الوحدة
- **الأدلة المُنفَّذة في هذه الجولة:**
  - `npx eslint src/modules/reservations src/libs/decorators src/libs/utils/postgres-error.ts test/unit/reservations` → **نجح**
  - `npx jest test/unit/reservations --runInBand` → **مجموعتان / 13 اختبارًا ناجحًا** (كانت 11؛ أُضيف اختبارا «ترجمة 23505» و«إعادة رمي الأخطاء غير المرتبطة»)
  - لم يُنفَّذ: `npm run build`، اختبارات التكامل، تشغيل الهجرة على قاعدة بيانات.
- **الجولة السابقة:** 14 ملاحظة (S1–S14). هذه الجولة تعيد التحقق من كل منها وتضيف ملاحظات جديدة (N1–N6).

---

## الخلاصة

تكرار v2 استجابة حقيقية وموجّهة بشكل جيد لملاحظات الجولة الأولى: سباق الحجز المزدوج الحرج أُغلق الآن على مستوى قاعدة البيانات (فهرس فريد جزئي + ترجمة الخطأ + اختبارات)، والخدمة تتبع اصطلاح الـ Base Service في الوحدات، والمتحقق أصبح واعيًا بالتقويم والمنطقة الزمنية، ورسائل التحقق مترجمة كبقية التطبيق. **لكن العائقَين المتبقيين من الجولة الأولى (عقد الـ API بالكيان الخام، وارتباط الخدمة بالمزوّد) ما زالا قائمَين**، كما أدخلت الإصلاحات بضع ملاحظات أصغر تستحق المعالجة قبل الدمج.

---

## تتبّع الجولة الأولى ← الثانية

| المعرّف | ملاحظة الجولة الأولى | الحالة في v2 | ملاحظات |
|----|-----------------|--------------|---------|
| S1 | لا يوجد قيد فريد في DB → سباق الحجز المزدوج | **حُلّت** | فهرس فريد جزئي `UQ_RESERVATIONS_SHIFT_DATE_ACTIVE`، وترجمة 23505 إلى `ConflictException`، مع اختبارات. راجع N1/N2 للاستكمال. |
| S2 | إرجاع الكيان الخام دون Response DTO/Mapper | **ما زالت قائمة** | الـ Controller لا يزال يعيد `ReservationEntity` مباشرة. |
| S3 | ارتباط الخدمة بالمناوبة/المزوّد غير قابل للتحقق | **ما زالت قائمة** | فجوة نموذج تحتاج قرار منتج. |
| S4 | فحص التاريخ كان بالمنطقة المحلية + مقارنة نصية | **حُلّت** (راجع N5) | الآن بتطبيع UTC مع فحص التقويم. مرساة المنطقة الزمنية تستحق تأكيدًا. |
| S5 | القيمة الافتراضية للحالة غير منعكسة في الكيان | **حُلّت** | أُضيف `default: ReservationStatusEnum.Pending`. |
| S6 | `ON DELETE SET NULL` يمحو سجل الحجز التاريخي | **ما زالت قائمة** | قرار سياسة. |
| S7 | `workerId` غير مستخدم؛ لا يوجد نموذج سعة | **ما زالت قائمة** | قرار منتج. |
| S8 | اختبار «الخدمة غير النشطة» مضلل | **حُلّت** | أُعيدت تسميته بدقة؛ فرع «غير النشطة» غير قابل للوصول المنفصل أصلًا بسبب فلتر `isActive: true`. |
| S9 | لا توجد اختبارات تكامل | **ما زالت قائمة** | النمط موجود في المستودع (`test/integration/*`). |
| S10 | `notes` بحد `@MinLength(50)` تعسّفي | **ما زالت قائمة** | |
| S11 | تناقض 404 مقابل 400 (خدمة/مناوبة غير نشطة) | **ما زالت قائمة** | |
| S12 | رسائل تحقق إنجليزية ثابتة | **حُلّت** | أُضيفت `common.validation.*` بالعربية والإنجليزية؛ وتتماشى مع نمط `I18nContext.current()` المستخدم في Mappers الترجمة. |
| S13 | Controller واحد / مسار عام + لا Rate Limit | **ما زالت قائمة** | أولوية منخفضة حتى وجود تدفقات المزوّد/الإدارة. |
| S14 | لا سطر جديد بنهاية الملف / ارتجال الـ pre-push | **جزئية** | `shift.module.ts` أصبح سليمًا؛ `reservations.module.ts` ما زال بلا سطر جديد؛ وارتجالات التاريخ ما زالت في السجل (على مستوى git). |

---

## ملاحظات جديدة (أظهرتها/كشفتها v2)

| المعرّف | الخطورة | الموقع | الخلاصة |
|----|----------|----------|---------|
| N1 | متوسطة | هجرة `...0001` | تحذف فهرسي القوائم (`providerId,date,status` و`clientId,status`)؛ وهما الأدعى لاستعلامَي القائمة الأكثر ترجيحًا مستقبلًا |
| N2 | منخفضة–متوسطة | التقاط `save()` في الخدمة | ترجمة انتهاك التفرد تتجاهل `driverError.constraint` — أي فهرس فريد مستقبلي سيُبلَّغ خطأً على أنه «تعارض حجز» |
| N3 | منخفضة–متوسطة | `getNotFoundMessage()` في الخدمة | يعيد `reservations.errors.serviceNotFound` — دلالة خاطئة لرسالة «غير موجود» الخاصة بالحجز (مفتاح منسوخ) |
| N4 | متوسطة | الاختبارات | المتحقق المشترك المُعاد كتابته (`IsNotPastDate`) **بلا اختبارات وحدة مباشرة** لمنطقه الجديد (فحص التقويم، مقارنة UTC، الرسالة المترجمة) |
| N5 | منخفضة | `IsNotPastDate` | مرساة UTC تسمح بحجز «أمس الرياض» في نافذة 21:00–24:00 UTC؛ تأكيد المنطقة الزمنية التجارية المقصودة |
| N6 | منخفضة (استباقية) | الكيان/الفهرس | الحجز المحذوف ناعمًا (أو المكتمل مستقبلًا) بحالة نشطة يظل محتكرًا للفتحة — يجب أن يضبط تدفق الإلغاء/الأرشفة القادم الحالة على `Cancelled` (شرط الفهرس يستثني `Cancelled` فقط) |

ملاحظة خارج النطاق: `app.module.ts` يسجّل `I18nModule.forRoot({...})` **مرتين** بنفس الخيارات (L85–104). سابقة الوجود (ليست من هذا الفرع) لكن يُستحسن تنظيفها لأن توطين التحقق يعتمد الآن على هذه الوحدة.

---

## الملاحظات ملفًا ملفًا (حالة v2 الحالية)

### 1) `reservations.entity.ts` — تحسّن مع ملاحظتين
- **S5 حُلّت:** `default: ReservationStatusEnum.Pending` أصبح على عمود `status` — الكيان والهجرة متطابقان.
- **S1 حُلّت:** `@Index('UQ_RESERVATIONS_SHIFT_DATE_ACTIVE', ['shiftId','date'], { unique: true, where: ... })` يوثّق الضمانة التي تنشئها الهجرة.
- **N6 (ملاحظة تصميم):** شرط الفهرس الجزئي يستثني `status = 'Cancelled'` فقط. الحجز المحذوف ناعمًا (`deletedAt` مضبوط عبر `AbstractEntityWithDeletedAt`) بحالة `Pending`/`Confirmed` **سيظل محتكرًا للفتحة**. عند وصول Endpoints الإلغاء/الأرشفة يجب ضبط `status = Cancelled` (أو أن يستثني الشرط `deletedAt IS NOT NULL` أيضًا). يستحق تعليقًا على الفهرس.
- **S3/S6/S7 قائمة** كما في الجولة الأولى (انظر التتبّع).

### 2) الهجرة `1786000000001-add-reservations-shift-date-active-unique-index.ts`
- الاتجاه صحيح: هجرة بنمط إضافي مع `down()` قابل للعكس سليمًا (تحذف الفهرس الفريد وتعيد إنشاء الفهرسَين الأصليين).
- **N1 — متوسطة:** `up()` **يحذف** `IDX_RESERVATIONS_PROVIDER_DATE` و`IDX_RESERVATIONS_CLIENT` بدلًا من الإبقاء عليهما إلى جانب الجديد.
  - لماذا يهم: هذان هما الفهرس الطبيعيان لمسارَي القراءة الحتميَّين — «لوحة المزوّد: حجوزاتي بالتاريخ» (`providerId, date, status`) و«العميل: سجلي» (`clientId, status`). والفهرس الفريد الجديد يخدم مسار الإنشاء/التعارض فقط (`shiftId, date`).
  - الحل: الإبقاء عليهما (موصى به — الجدول صغير وكلفة الكتابة لا تُذكر وتلك القوائم هي الميزات التالية الأرجح)، أو تأجيل حذفهما بوعي حتى وجود استعلامات القائمة وقياسها. وإن أُبقيا فأعد إعلانهما على الكيان ليبقى متطابقًا مع الهجرة.
- المسار السريع لفحص التعارض `findOne` على `(shiftId, date, status ≠ Cancelled)` أصبح مغطى ببادئة الفهرس الفريد — جيد.

### 3) `reservations.service.ts` — تحسّن بنيوي
- أصبح يمتد `BaseEntityService<ReservationEntity>` (`super(repository, i18n)`) ويستخدم `this.findOneBy(...)` في الفحص المسبق — متناسق مع اصطلاحات الوحدات.
- **S1 (منطقيًا):** `save()` مغلَّف:
```ts
try {
  return await this.repository.save(reservation);
} catch (error) {
  if (isPostgresUniqueViolation(error)) {
    throw new ConflictException(this.i18n.t('reservations.errors.conflict'));
  }
  throw error;
}
```
  الفكرة صحيحة؛ ويبقى الفحص المسبق `findOne` مسارًا سريعًا رخيصًا والفهرس هو المرجع. تحسينان:
  - **N2:** تحقق أيضًا من اسم القيد — `isPostgresUniqueViolation` يفحص `driverError.code` فقط. إذا أُضيف أي فهرس فريد ثانٍ لجدول `reservations` مستقبلًا، سيُبلَّغ كل انتهاك على أنه «يوجد حجز مسبق…». الأداة تكشف `constraint` أصلًا فاستخدمها:
    ```ts
    if (
      isPostgresUniqueViolation(error) &&
      getPostgresDriverError(error)?.constraint === 'UQ_RESERVATIONS_SHIFT_DATE_ACTIVE'
    ) { ... }
    ```
  - اختياري: الفحص المسبق يضاعف جولات DB في المسار السعيد (findOne ثم إدراج). مقبول؛ وإن سخُن هذا الـ Endpoint لاحقًا فحذف الفحص والاعتماد على ترجمة 23505 يخفض القراءات للنصف.
- **N3:** `protected getNotFoundMessage()` يعيد `reservations.errors.serviceNotFound`. أي استدعاء مستقبلي لـ `findOneByOrFail` على مستودع الحجوزات سيُظهر «الخدمة غير موجودة أو غير متاحة» لحجز مفقود. أضف مفتاح `reservations.errors.notFound` (عربي/إنجليزي) وأعده هنا.
- **S3/S7/S11 قائمة** (ارتباط الخدمة بالمزوّد، العامل/السعة، دلالات 404 مقابل 400).

### 4) `reservations.controller.ts` — دون تغيير (S2 قائمة)
- ما زال يعيد `ReservationEntity` الخام داخل `successResponse`. توصية الجولة الأولى قائمة: أضف `dto/response/reservation-response.dto.ts` مع Mapper صغير (محلي أو في `src/libs/mappers`)، وقيّد `@ApiCreatedResponse` به، وأعد الـ DTO المُحوَّل. لا يوجد `ClassSerializerInterceptor` هنا، لذا فإن `@Exclude`/تنسيق التواريخ في الكيانات المجرّدة لا يعمل أبدًا وحمولة اليوم هي ما يحتويه الكيان (بما فيه `cancellationReason: null`).
- **S13:** ما زال `@Controller('reservations')` واحدًا؛ مقبول ما دام العملاء هم الفاعلون الوحيدون. افصل Controllers الإدارة/المزوّد عند قدوم تلك التدفقات، وفكّر في `@RateLimit` للإنشاء.

### 5) `src/libs/utils/postgres-error.ts` — جديد وجيد
- صغير ومقيّد النوع ودفاعي (الوصول إلى `driverError` محروس). الأداتان `getPostgresDriverError` و`isPostgresUniqueViolation` قابلتان لإعادة الاستخدام. اقتراح (N2): أضف مساعدًا باسم القيد (`isUniqueViolation(error, constraint)`) ليتسنى للمتصلين الدقة.

### 6) `is-not-past-date.decorator.ts` — أُعيدت كتابته (S4 حُلّت مع N4/N5)
- الآن: يرفض غير `YYYY-MM-DD`، ويتحقق من التواريخ التقويمية الفعلية عبر round-trip بـ UTC (مثلًا `2026-02-30` يفشل)، ويقارن مع «اليوم» المُطبَّع بـ UTC — حتمي ومتين.
- **N5:** المرساة UTC لكن السوق المستهدف هو السعودية (UTC+3). بين 21:00–24:00 UTC (00:00–03:00 الرياض) يمكن للعميل حجز تاريخ هو «أمس» في الرياض لأن يوم UTC التقويمي لم ينقلب بعد. إذا كان تاريخ الحجز يعني «اليوم المحلي للنشاط التجاري»، فثبّت المرساة على المنطقة التجارية عبر `date-fns-tz` مثلًا:
  ```ts
  import { startOfDay } from 'date-fns-tz';
  const todayStart = startOfDay(new Date(), { timeZone: 'Asia/Riyadh' });
  ```
  أيُّ خيار دفاعي مقبول — اجعله صريحًا وثابتًا.
- **N4:** هذا منطق مشترك في `src/libs` وله حالات حافة حقيقية، لكن لا يوجد اختبار يمارسه (الـ DTO لا يُمرَّر عبر `ValidationPipe` في اختبارات الوحدة أصلًا). أضف اختبارات وحدة للديكوريتر (أو اختبارات تحقق على مستوى الـ DTO): رفض التاريخ الماضي، قبول اليوم/المستقبل، رفض الصيغ الخاطئة، رفض التواريخ المستحيلة كـ `2026-02-30`، وتحقق أن `defaultMessage` المترجم يُحل تحت `I18nContext`.

### 7) `is-bigint-id.decorator.ts` — رسالة مترجمة (S12 حُلّت)
- `defaultMessage` يفضّل الآن `I18nContext.current()?.t('common.validation.bigIntId', { args: { property } })` مع الإبقاء على الإنجليزية كاحتياط. يتماشى مع النمط السائد في المستودع (كل Mappers الترجمة تستخدم `I18nContext.current()`)، وnestjs-i18n 10.8.4 يربط السياق لكل طلب عبر AsyncLocalStorage — فالمنهج سليم. خارج سياق الطلب (اختبارات وحدة خام) يُستخدم الاحتياط؛ وهذا مقبول ولهذا وُجد أصلًا.
- `common.json` بالعربية والإنجليزية يعرّفان `validation.bigIntId` و`validation.notPastDate` مع placeholder `{property}`. متناسق.

### 8) الـ DTO وربط الوحدات و i18n
- `create-reservation.dto.ts`: دون تغيير — **S10 قائمة** (`notes` اختياري لكن `@MinLength(50)` يرفض ملاحظات قصيرة مشروعة). ما عداه سليم (ديكوراتيرات BIGINT، تاريخ صارم + `@Matches`، توثيق Swagger).
- `reservations.module.ts`: الاستيرادات/التصديرات صحيحة. **S14:** ما زال بلا سطر جديد بنهاية الملف.
- `shift.module.ts`: `exports: [ShiftService]` — صحيح، والسطر الأخير موجود.
- `reservations.json` (عربي/إنجليزي): مفاتيح متوازنة وبلا فجوات.

### 9) الاختبارات
`reservations.service.spec.ts` (الإجمالي الآن 13 اختبارًا عبر الملفّين):
- **S8 حُلّت:** أعيدت تسمية الاختبار المكرر إلى «الخدمة غير متاحة أو غير نشطة» — دقيق، لأن فلتر `isActive: true` يجعل الحالتين متطابقتين سلوكيًا (لا يوجد فرع منفصل للاختبار). الفلتر نفسه مُثبت في اختبار المسار السعيد.
- **S1 مُتحقق:** اختباران جديدان لترجمة `23505` → `ConflictException` ولإعادة رمي الأخطاء غير المرتبطة كما هي. جيد.
- ناقص (ترحيل + N4): حالة `notes` الافتراضية `null`؛ اختبارات مباشرة لمنطق المتحقق الجديد؛ اختبار تكامل.

`reservations.controller.spec.ts`:
- دون تغيير — المسار السعيد فقط. أضف: انتشار استثناءات الخدمة (`404`/`409`/`403`) وشكل الاستجابة مع `201`.

**تغطية التكامل (S9) — ما زالت قائمة.** أضف `test/integration/reservations/reservations.api.spec.ts` على غرار specs الموجودة (`provider`/`languages`/`skills`). الحالة الأعلى قيمة هي التي لا تستطيع اختبارات الوحدة إثباتها: **إنشاءان متزامنان لنفس المناوبة+التاريخ → نجاح واحد فقط** (يمارس الفهرس الفريد الجزئي الحقيقي من النهاية للنهاية).

---

## نظافة Git (دون تغيير عن الجولة الأولى)
- الالتزامان `c8d4d41`/`e662700` («bug: test the pre push») يعدّلان `.husky/pre-push` فقط وحصيلتهما صفر تغيير مقابل `6b0639a`. احذفهما/ادمجهما قبل الدمج.

---

## ترتيب الإصلاحات المقترح (الجولة الثانية)

1. **N1:** حسم سياسة الفهارس — الإبقاء على `(providerId, date, status)` و`(clientId, status)` (موصى به) أو التأجيل بوعي؛ مع إبقاء الكيان متطابقًا مع الهجرة.
2. **S2:** Response DTO + Mapper وتوثيق Swagger بالنوع.
3. **S3/S7:** حسم دلالات الخدمة↔المزوّد↔المناوبة وسعة المناوبة؛ وترجمتها إلى تحقق واختبارات.
4. **N2/N3:** ترجمة 23505 مقيّدة باسم القيد؛ ومفتاح `reservations.errors.notFound` صحيح في `getNotFoundMessage()`.
5. **N4:** اختبارات وحدة لـ `IsNotPastDate`؛ **N5:** تأكيد مرساة المنطقة التجارية.
6. **S9:** اختبار تكامل يتضمن تأكيد التزامن.
7. **S6، S10، S11، S13، S14:** بقية بنود السياسة والتنظيف.

## ملخص التحقق

| الأمر | النتيجة |
|---|---|
| `npx eslint <الملفات المتغيّرة>` | نجح |
| `npx jest test/unit/reservations --runInBand` | نجح — مجموعتان، 13 اختبارًا |
| `npm run build` / اختبارات التكامل / تشغيل الهجرة | لم تُنفَّذ |
